01 - VOLUME_CONCILIADO = SUM(TB_CONCILIACAO_HUB_DMENOS1[Saldo_Logix])

02 - VOL_DIVERGÊNCIAS_D-1 = ABS(SUM(TB_CONCILIACAO_HUB_DMENOS1[Diferenca02]))

03 - QTD_DIVERGENCIAS = 
var div = 
COUNTA(TB_CONCILIACAO_HUB_DMENOS1[Status_Divergencias])

RETURN

IF(div = 0, 0 , div )

04 - QTD_RETROATIVO = COUNTA(TB_OCORRENCIAS_HUB_DMENOS1[STATUS])

05 - 


