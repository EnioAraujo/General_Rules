01 - REPLACE(RTRIM(LTRIM(DES_FUNCAO)), 'PICKING', 'ARMAZEM') AS DES_FUNCAO_COPIA

02 - REPLACE(RTRIM(LTRIM(ITEM)), 'N', '')              AS ITEM,

03 - ISNULL(RTRIM(LTRIM(CTR_ENT_SAI_VEIC_DOCUM)), '-') AS CTR_ENT_SAI_VEIC_DOCUM

04 -  ISNULL(RTRIM(LTRIM(NUM_LOTE)), 'SEM LOTE')        AS NUM_LOTE, 

05 - 
            CASE WHEN RTRIM(LTRIM(DEN_SITUA_QTD)) = 'IMPEDIDO' THEN CAST(QTD_SALDO_FISICO AS FLOAT) ELSE 0 END AS SALDO_BLOQUEADO,


            CASE WHEN RTRIM(LTRIM(DEN_SITUA_QTD)) = 'LIBERADO' THEN CAST(QTD_RESERVA_NORMAL AS FLOAT) ELSE 0 END AS SALDO_RESERVADO





